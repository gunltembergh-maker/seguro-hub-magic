// Placeholder — implementação chega no próximo bloco.
import { EstadoVazio } from "@/components/analise-background/AbBits";

export default function FontesGatilhos(_props: Record<string, unknown>) {
  return (
    <EstadoVazio
      titulo="Em construção"
      detalhe="Esta aba será entregue no próximo bloco do módulo Análise Background."
    />
  );
}
